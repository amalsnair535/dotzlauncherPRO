# 1. Add all changes (About screen, License, Website docs)
git add .

# 2. Commit the release
git commit -m "release: v5.5.0 - Multi-Flavor Support & UI Stability"

# 3. Tag the release (useful for 'Check for Updates' logic)
git tag -a v5.5.0 -m "Release version 5.5.0"

# 4. Push to GitHub (This updates BOTH the repo and the website)
git push origin main
git push origin v5.5.0# 1. Add all changes (About screen, License, Website docs)
git add .

# 2. Commit the release
git commit -m "release: v5.5.0 - Multi-Flavor Support & UI Stability"

# 3. Tag the release (useful for 'Check for Updates' logic)
git tag -a v5.5.0 -m "Release version 5.5.0"

# 4. Push to GitHub (This updates BOTH the repo and the website)
git push origin main
git push origin v5.5.0